# TutorMark 제작 실습 프롬프트

> 강의 실습용. 아래 **메인 프롬프트**를 Claude Code / Cursor / ChatGPT 등에 그대로 붙여넣으면
> 동작하는 사진 과제 첨삭 웹앱이 만들어집니다.

---

## 📋 메인 프롬프트 (전체 한 번에)

````
너는 시니어 풀스택 개발자다. 아래 명세대로 **실제로 동작하는** 웹 애플리케이션을 처음부터 만들어라.
설명만 하지 말고 파일을 실제로 생성하고, 마지막에 서버를 띄워 동작을 확인해라.

## 1. 만들 것

줌(Zoom) 원격 과외·멘토링 수업용 **사진 과제 첨삭 시스템** "TutorMark".

- 학생이 스마트폰으로 시험지·문제집을 촬영해 여러 장 업로드한다.
- 선생님이 그 사진을 화면에서 확대하고, 마우스로 펜/형광펜 첨삭을 하거나
  키보드로 글자를 입력해 주석을 단다.
- 첨삭한 결과를 원본 해상도 이미지로 합성해 저장하고, 총평 코멘트와 함께
  학생에게 댓글 형태로 보여준다.

## 2. 기술 스택 (그대로 지킬 것)

- 백엔드: Python 3.11 + FastAPI + uvicorn
- DB: Supabase(PostgreSQL) 우선 / `.env` 미설정 시 로컬 SQLite 자동 폴백
- 파일 저장: Supabase Storage 우선 / 미설정 시 로컬 `uploads/` 폴더
- 프런트엔드: 단일 HTML + 순수 JavaScript (빌드 도구·프레임워크 없음)
  + Tailwind CSS CDN + Lucide 아이콘 CDN
- 이미지 처리: Pillow (+ pillow-heif로 아이폰 HEIC 지원)
- 캔버스: **외부 드로잉 라이브러리 금지.** Canvas 2D API로 직접 구현한다.
- UI 문구는 전부 한국어.

## 3. 파일 구조

```
app.py                  FastAPI 진입점 + API
database.py             SQLite 로컬 저장소
supabase_service.py     Supabase 연동 + 로컬 폴백 분기
index.html              전체 UI (단일 페이지)
static/js/app.js        화면 제어 · 데이터 로딩
static/js/canvas-editor.js  캔버스 첨삭 엔진 (독립 클래스)
static/css/styles.css   커스텀 스타일
supabase_schema.sql     Supabase 테이블 + RLS + 버킷 생성 SQL
requirements.txt / .env.example / README.md
```

## 4. 데이터 모델

**students**: id, name, grade, pin, avatar_color, created_at
**submissions**: id, student_id, student_name, subject, title, memo,
  image_url(첫 장), image_urls(전체 장 배열), status('pending'|'reviewed'), created_at
**feedbacks**: id, submission_id, **page_index**(몇 번째 사진인지, 0부터),
  teacher_name, comment, annotated_image_url, **annotation_data**(벡터 JSON), created_at

- submissions 삭제 시 feedbacks도 함께 삭제(ON DELETE CASCADE).
- SQLite에서는 `PRAGMA foreign_keys = ON`을 반드시 켜야 CASCADE가 동작한다.

## 5. API

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/system/status` | 현재 저장 모드와 클라이언트용 Supabase 설정 |
| GET/POST | `/api/students` | 학생 목록 / 학생 추가 |
| GET | `/api/submissions?student_id=&status=` | 과제 목록 (첨삭 건수 포함) |
| GET | `/api/submissions/{id}` | 과제 상세 + 첨삭 목록 |
| POST | `/api/submissions` | multipart, 사진 여러 장 동시 업로드 |
| DELETE | `/api/submissions/{id}` | 과제 + 첨삭 + 저장된 파일까지 삭제 |
| POST | `/api/feedbacks` | 첨삭본(base64) + 총평 저장 |
| DELETE | `/api/feedbacks/{id}` | 첨삭 삭제 |
| GET | `/api/stats` | 대시보드 카운터 4종 |
| POST | `/api/teacher/verify-pin` | 교사 모드 PIN 확인 |

## 6. 화면

**학생 모드**
- 학생 프로필 칩 선택 + 학생 추가
- 사진 촬영/선택 (여러 장, 계속 추가 가능, 선택한 사진 썸네일과 개별 삭제)
- 과목/제목/메모 입력 후 제출
- 내 과제 목록 (전체·첨삭완료·대기중 필터), 클릭 시 상세 모달에서 첨삭 댓글 열람

**교사 모드** (PIN 입력 후 진입, 기본 1234)
- 대시보드 카운터 4종 (전체·미첨삭·첨삭완료·학생수)
- 학생별 필터, 과제 카드 목록
- [첨삭하기] → 전체화면 캔버스 에디터

**캔버스 첨삭 에디터** (전체화면 모달)
- 좌측 도구 팔레트: 펜 / 형광펜 / 텍스트 / 손도구, 채점 스탬프(⭕❌✔️),
  도형(화살표·사각형·원·직선), 지우개, 색상 8종 + 커스텀, 굵기·글자크기 슬라이더
- 상단: 페이지 넘김(N/M), 확대·축소·화면맞춤·100%, 실행취소/다시실행/전체지우기
- 하단: 총평 코멘트 입력 + [저장]

**이미지 라이트박스**: 사진 클릭 시 최상단 레이어에서 확대/드래그/다운로드

## 7. 캔버스 에디터 상세 명세 (가장 중요 — 여기서 대부분의 버그가 난다)

### 7-1. 데이터 모델
첨삭은 픽셀이 아니라 **벡터 객체 배열**로 관리한다.
```js
{ id, type:'pen'|'highlighter'|'text'|'line'|'arrow'|'rect'|'circle'
        |'grade-o'|'grade-x'|'grade-check',
  color, width, opacity, points:[{x,y}...], startX, startY, endX, endY,
  text, fontSize, x, y, size }
```
- 좌표는 **화면 좌표가 아니라 원본 이미지 좌표계**로 저장한다.
  그래야 확대/이동해도 첨삭이 이미지에 붙어 있고, 원본 해상도로 내보낼 수 있다.
- `screenToImage()` / `imageToScreen()` 변환 함수를 만들고 모든 입력을 이걸로 통과시켜라.

### 7-2. 히스토리 (실행취소/다시실행)
- **스냅샷 방식으로 구현하라.** 매 변경마다 `objects` 배열 전체를 복사해 스택에 넣고,
  `historyIndex`를 앞뒤로 움직여 되돌린다.
- 마지막 객체를 `pop()` 하는 방식은 금지. **지우개와 전체지우기를 되돌릴 수 없기 때문이다.**
- 지우개는 드래그 한 번에 히스토리 1칸만 쌓아라 (마우스 이동마다 쌓지 말 것).
- 단축키: Ctrl+Z(취소), Ctrl+Shift+Z / Ctrl+Y(다시실행).

### 7-3. 포인터 입력
- `pointerdown/move/up`을 쓰고, 그리기 시작 시 **`setPointerCapture()`를 반드시 호출**하라.
  이게 없으면 캔버스 밖으로 드래그했을 때 `pointerup`을 못 받아 획이 사라지고
  그리기 상태가 멈춘 채로 남는다.
- 마우스 휠 = 커서 위치 기준 확대/축소. 가운데/오른쪽 버튼 드래그 또는 Space+드래그 = 화면 이동.
  오른쪽 버튼으로 이동을 지원하므로 캔버스 안에서는 `contextmenu`를 막아라.
- 태블릿 지원: 두 손가락 핀치로 확대/축소. 두 번째 손가락이 닿으면 그리던 획은 취소한다.

### 7-4. 텍스트 입력 (특히 주의)
클릭한 위치에 `contentEditable` 오버레이를 띄워 바로 타이핑하게 한다. 아래를 반드시 지켜라.
- **포커스는 `requestAnimationFrame`으로 다음 프레임에 줘라.** 같은 틱에 `focus()`하면
  브라우저의 mousedown 기본 동작이 포커스를 뺏어가고, `blur` 핸들러가 빈 문자열로
  확정해버려 **글자가 절대 입력되지 않는다.** pointerdown에서 `preventDefault()`도 함께 호출한다.
- **전역 단축키 핸들러는 `isContentEditable`도 입력 요소로 취급해야 한다.**
  안 그러면 Space가 "화면 이동" 단축키로 가로채여 **띄어쓰기를 칠 수 없다.**
- Enter=확정, Shift+Enter=줄바꿈, Esc=취소. **Esc로 취소하면 저장되면 안 된다**
  (요소를 지우면 blur가 발생해 확정 핸들러가 또 돌 수 있으니 플래그로 1회만 처리).
- 전역 키보드 핸들러는 **에디터가 열려 있을 때만** 동작해야 한다.
  모달이 닫힌 상태에서 페이지 전체의 Space/Ctrl+Z를 가로채면 안 된다.
- 확정된 텍스트는 가독성을 위해 흰 반투명 배경 박스를 깔고 그 위에 그린다.

### 7-5. 내보내기
- 화면 캔버스가 아니라 **원본 이미지 크기의 오프스크린 캔버스**를 새로 만들어
  배경 이미지 + 모든 객체를 1:1로 다시 그린 뒤 JPEG로 뽑는다.
- 이미지 로드시 `img.crossOrigin = 'anonymous'`를 설정하라. 없으면 외부(Supabase) 이미지에서
  캔버스가 오염(taint)되어 `toDataURL()`이 **예외를 던진다.** 이 예외는 잡아서
  "CORS 설정을 확인하세요"라는 한국어 메시지로 바꿔라.
- 이미지와 함께 **벡터 JSON(`annotation_data`)도 같이 저장**한다. 나중에 재첨삭에 쓴다.

### 7-6. 여러 장(다중 페이지) 처리
- 에디터 클래스에 `exportVectorData()` / `importVectorData()` / `clearDrawingsOnly()`를
  **반드시 구현하라.** 페이지 전환은 이 API로만 한다.
- 페이지를 넘기기 전에 현재 페이지의 첨삭과 총평 코멘트를 캐시에 저장하고,
  돌아오면 그대로 복원한다.
- 저장 시 마지막 장이 아니면 **에디터를 닫지 말고 다음 페이지로 자동 이동**한다.
- 이미 첨삭한 과제를 다시 열면 저장된 `annotation_data`를 불러와 **이어서 수정**할 수 있게 한다.
- 화면 크기와 devicePixelRatio에 맞춰 캔버스 백킹 스토어를 잡아 고해상도에서도 선명하게.

## 8. 반드시 지킬 구현 규칙

1. **Supabase / 로컬 폴백 판단**
   - 우선순위: ① 브라우저에 저장된 사용자 지정 키 → ② 서버 `/api/system/status` 응답 → ③ 정적 기본값.
   - **"결과가 0건이면 다른 소스로 폴백" 같은 로직은 절대 금지.**
     "대기중 0건"은 정상 응답인데 로컬 DB의 엉뚱한 데이터가 표시된다.
     성공/실패를 나타내는 별도 플래그로 분기하라.
   - 서버가 "로컬 모드"라고 응답하면 그게 최종 결정이다. 정적 기본 키로 몰래 접속하지 마라.

2. **에러 코드 보존**
   - `try` 블록 안에서 `raise HTTPException(404)` 해놓고 `except Exception`으로 다시 감싸면
     **404가 500으로 둔갑한다.** `except HTTPException: raise`를 먼저 두어라.

3. **HTML 생성 시 이벤트 연결**
   - `onclick="fn('${제목}')"` 처럼 문자열을 끼워넣지 마라. 제목에 따옴표가 있으면
     깨지고 스크립트 주입 통로가 된다.
   - `data-*` 속성 + 이벤트 위임(`closest()`)으로 처리하고, 속성값은 이스케이프하라.

4. **자원 정리**
   - 과제 삭제 시 DB 행뿐 아니라 **저장된 이미지 파일도 지워라.** Supabase Storage는
     파일명만이 아니라 `feedbacks/xxx.jpg` 같은 **버킷 내 전체 경로**로 지워야 한다.
   - 미리보기 썸네일의 `URL.createObjectURL()`은 다시 그릴 때 `revokeObjectURL()`로 해제하라.
   - 로컬 파일 삭제 시 경로가 `uploads/` 밖으로 나가면 거부하라(경로 탈출 방지).

5. **이미지 처리**
   - 업로드 시 `ImageOps.exif_transpose()`로 스마트폰 사진 회전을 보정하고,
     긴 변 2000px로 축소한 WebP(품질 70%, method=6)로 정규화한다. RGBA/P 모드는 RGB로 변환.
   - 파일당 25MB, 1회 30장 제한. 인식 불가 형식은 400으로 한국어 안내.

6. **보안**
   - `/api/system/status`가 키를 브라우저로 내려줄 때 **JWT의 `role` 클레임을 검사해
     `anon` 키일 때만 내려보내라.** service_role 키가 노출되면 누구나 DB 전권을 갖는다.
   - `supabase_schema.sql`의 RLS를 `USING (true)`로 만들었다면, README에
     "주소를 아는 누구나 모든 데이터를 읽고 지울 수 있다"는 경고를 명시하라.

7. **UI**
   - 첫 데이터 로딩이 수 초 걸릴 수 있으니 로딩 표시를 넣어라(빈 화면 금지).
   - 저장하지 않은 첨삭이 있는데 닫거나 새로고침하면 경고한다.
   - 모바일 접근성을 위해 `user-scalable=no`는 쓰지 마라.

## 9. 완료 기준

아래가 전부 동작해야 완료다. 직접 실행해서 확인하고 결과를 보고하라.

- [ ] `.env` 없이 `python app.py` → SQLite 모드로 즉시 구동, 브라우저에 "로컬 모드" 표시
- [ ] 사진 3장 선택 → 썸네일 3개 → 제출 → 목록에 "3장" 카드 생성
- [ ] 교사 모드 PIN 1234 진입, 대시보드 카운터 표시
- [ ] 캔버스: 펜으로 그리기, **캔버스 밖으로 드래그해도 획이 남는지**
- [ ] 캔버스: 텍스트 도구로 **띄어쓰기가 들어간 문장**이 입력·저장되는지
- [ ] 캔버스: 지우개로 지운 뒤 Ctrl+Z로 **복구되는지**, 전체지우기도 되돌려지는지
- [ ] 캔버스: 다음 페이지 → 이전 페이지로 돌아왔을 때 첨삭이 **그대로 남아 있는지**
- [ ] 저장 후 학생 화면 댓글에 첨삭본 이미지와 총평이 보이는지
- [ ] 과제 삭제 후 `uploads/` 폴더에 이미지 파일이 **남아 있지 않은지**
- [ ] 브라우저 콘솔에 에러가 하나도 없는지
````

---

## 🪜 단계별 분할 버전 (수업 진행용)

한 번에 다 만들면 학생이 따라오기 어렵습니다. 4단계로 끊어서 진행하세요.

### 1단계 — 뼈대와 데이터 (약 20분)
> 위 프롬프트의 **1~6장**만 붙여넣고 이렇게 덧붙입니다.
>
> "일단 **캔버스 첨삭 기능은 빼고** 만들어라. 사진 업로드 → 목록 표시 →
> 상세 보기까지만 동작하면 된다. 첨삭 버튼은 자리만 만들어두고 클릭하면
> '준비 중' 토스트를 띄워라."

### 2단계 — 캔버스 엔진 (약 40분, 핵심)
> "이제 `static/js/canvas-editor.js`에 캔버스 첨삭 엔진을 추가하라."
> 로 시작해 **7장 전체**를 붙여넣습니다. 단, 7-6(다중 페이지)은 뺍니다.

### 3단계 — 여러 장 첨삭 (약 25분)
> 7-6과 8장의 1·2번 규칙을 붙여넣습니다.
> **여기서 학생들이 가장 많이 막힙니다.** 페이지 캐시 구조를 칠판에 그려주세요.

### 4단계 — 배포와 보안 (약 20분)
> 8장의 4·6번 규칙 + Supabase 연동. RLS 정책을 직접 짜보게 하면 좋습니다.

---

## 🎓 강사용 노트 — 학생 결과물에서 반드시 나오는 버그

이 프롬프트의 안전장치를 **일부러 빼고** 만들게 한 뒤 디버깅시키면 훨씬 좋은 수업이 됩니다.
아래는 실제로 관찰된 빈출 버그입니다.

| # | 증상 | 원인 | 관련 프롬프트 조항 |
|---|---|---|---|
| 1 | 다음 페이지 버튼이 아무 반응 없음 | 호출만 하고 정의하지 않은 메서드 (`exportVectorData` 등) | 7-6 |
| 2 | 글자가 입력되지 않고 상자가 사라짐 | 같은 틱에 `focus()` → 기본 동작이 포커스 탈취 | 7-4 |
| 3 | **띄어쓰기가 안 됨** | Space를 전역 단축키가 가로챔 | 7-4 |
| 4 | 지우개로 지운 게 Ctrl+Z로 안 돌아옴 | `pop()` 기반 히스토리 | 7-2 |
| 5 | 캔버스 밖으로 드래그하면 획이 증발 | `setPointerCapture()` 누락 | 7-3 |
| 6 | 필터가 0건일 때 남의 데이터가 뜸 | `if (length === 0)` 폴백 | 8-1 |
| 7 | 없는 과제 조회했는데 500 에러 | `except Exception`이 404를 삼킴 | 8-2 |
| 8 | 제목에 따옴표 넣으면 클릭이 깨짐 | `onclick`에 문자열 보간 | 8-3 |
| 9 | 삭제해도 서버에 사진이 계속 쌓임 | 파일 정리 누락 | 8-4 |
| 10 | 첨삭본 저장 버튼이 조용히 실패 | CORS로 캔버스 오염 → `toDataURL()` 예외 | 7-5 |

**추천 진행법**: 1단계·2단계는 안전장치를 뺀 프롬프트로 만들게 한 뒤,
"띄어쓰기를 쳐보세요" / "다음 페이지를 눌러보세요" 로 버그를 직접 체험시키고,
그다음에 해당 조항을 공개해 고치게 합니다. AI에게 **무엇을 명시해야 하는가**를
체감시키는 것이 이 실습의 핵심입니다.

---

## 🔍 검증 스크립트 제공

학생 결과물을 자동 채점하려면 이 프로젝트의 `verify_e2e.py`를 함께 배포하세요.
실제 브라우저를 띄워 59개 항목(위 완료 기준 포함)을 점검하고 통과 개수를 출력합니다.

```bash
python -m pip install playwright requests pillow
python -m playwright install chromium
python verify_e2e.py
```
